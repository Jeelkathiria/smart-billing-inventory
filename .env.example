# ============================================================================
# Smart Billing & Inventory - Environment Configuration
# ============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit .env file to version control!
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# For InfinityFree, these are provided in your Control Panel
# Go to: Control Panel → Databases → MySQL Info

DB_HOST=127.0.0.1
DB_USER=xxxxx_dbuser
DB_PASS=your-database-password
DB_NAME=xxxxx_dbname

# ============================================================================
# EMAIL CONFIGURATION (for OTP and Password Reset)
# ============================================================================
# If using Gmail, generate App Password instead of using main password
# Steps:
# 1. Enable 2-Factor Authentication on Google Account
# 2. Go to: myaccount.google.com → Security → App passwords
# 3. Select "Mail" and "Windows Computer" (or your device)
# 4. Copy the generated 16-character password and paste below

MAIL_SMTP_HOST=smtp.gmail.com
MAIL_SMTP_PORT=587
MAIL_SMTP_AUTH=true
MAIL_SMTP_SECURE=tls
MAIL_FROM_EMAIL=your-email@gmail.com
MAIL_FROM_NAME=BillMitra
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# APP_ENV options: development, production
# In production, use: production

APP_ENV=production
APP_NAME=Smart Billing & Inventory
APP_DEBUG=false

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================
# Session timeout in minutes
SESSION_TIMEOUT=30

# ============================================================================
# InfinityFree Specific Notes
# ============================================================================
# - Database host is usually 127.0.0.1
# - Get credentials from InfinityFree Control Panel
# - Use phpMyAdmin to import database.sql
# - Upload files to htdocs/ folder via FTP
# - Ensure config/db.php and config/mail.php are in .gitignore
